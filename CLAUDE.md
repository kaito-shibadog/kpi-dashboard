# CLAUDE.md — NINAITE AIエージェント基盤 共通前提

このファイルは、このリポジトリで動く**すべてのAIエージェント（部下）が共通して読む前提知識**です。
会社・セグメント・会計の定義など「どの部下にも知っておいてほしいこと」をここに集約します。

> ⚠️ **具体的な金額・実数はここに書かない**こと。ここは「定義」の置き場所であり、
> 生の財務数値は `data/`（gitignore済み）や Google Drive 側に置く。

---

## ミッション（最上位）

このリポジトリのすべてのエージェントは、最終的に **企業価値の向上（＝株価の向上）** に資するために存在する。

- 各エージェントは自らの成果について **「これは企業価値にどう効くか」** を常に接続して語ること。
- 優先順位の判断基準: 価値を **"作る"** 活動（成長・収益性・希薄化の抑制・エクイティストーリー）を **主**、
  価値を **"守る"** 活動（統制・監査・IPO準備・労務法務）を **従** とする。守りは必要だが衛生要因。
- **株価 ＝ 企業価値 ÷ 株数。** よって希薄化のコントロール（既存事業CFで開発投資をどこまで賄えるか）も
  企業価値向上のレバーとして扱う。

## 会社概要

- **会社名:** 株式会社NINAITE（札幌）
- **事業ドメイン:** 外国人雇用領域の4事業
- **性質:** 黒字のサービス/BPO事業 ＋ 未収益SaaS（ninaite.ai）の「二頭の会社」
- **資本:** シリーズA済（ポスト約11億）、次はシリーズB

## ビジネスモデルとキャッシュの構造

- 既存の黒字事業（直接雇用・派遣）が継続的にキャッシュを生む土台。その上に SaaS（ninaite.ai）の開発投資が乗る**「二頭」構造**。
- 単純なバーンレート型スタートアップとは異なり、現預金は **既存事業CF（月次で±に振れる）＋開発投資** の合算で増減する。
- 開発費はBS資産化方針のため、**PLは黒字でもCFはマイナスに出る**ことがある（PLとCFが乖離）。
- よって資金の見方は「単純なランウェイ」より、**CFの構造（既存事業が生むCF ↔ 開発が食うCF）を分離してモニタリング**する。

## 管理会計セグメント（3区分）

このリポジトリのエージェントは、常に以下の3セグメントで数字を分離して語る。

1. **直接雇用**
2. **派遣**
3. **ninaite.ai（SaaS）**

補足ルール:
- 学校（教育）は**原価側**として扱う
- 開発費は**BS資産化**（PLは黒字・CFはマイナスに出る）方針

> 各セグメントの詳細な科目マッピング・境界定義は [docs/semantic-layer.md](docs/semantic-layer.md) を参照。

## 北極星（ミッション達成の手段）

「黒字サービス事業」と「未収益SaaS」の**2つを分離して語れる管理会計・KPI基盤**を建てること。

これは単なる管理会計の整備ではなく、**SaaSに"SaaSのマルチプル"を付けさせるための武器**である。
人材・BPO事業は利益ベースの数倍で評価される一方、SaaSはARR成長×継続率で評価の桁が変わる。
**ninaite.ai が既存事業のPLに埋もれている限り、会社全体が"人材会社"として低いマルチプルで見られる。**
分離して語れること自体が、そのままバリュエーションの差になる。

その上にシリーズB評価・IR定量化・IPO準備・監査耐性が乗る。
「目の前を楽にする」より **複利資産の構築** を優先する。

## 源泉データ（4系統）

1. MF会計
2. 業績管理表
3. 経営管理表
4. HubSpot

## 進め方（メソッド）

- **ウォーターフォール禁止。** エージェント1体ごとに
  「契約定義 → 薄いデータ層 → 実装 → 検証 → 実戦1サイクル」を反復する。
- データ層は回すたびに厚くする。各論（セグメント/割賦など）はエージェントの仕様書であり、寄り道ではない。

## エージェント構築の順序

**企業価値への効き方**で優先順位を決める（価値を"作る"が主、"守る"が従）。

1. **月次予実アナリスト** — 土台。全社の数字を3セグメントで分離して語れる状態にする ← 実装1番手
2. **SaaS KPIアナリスト** — マルチプルの本丸（ARR成長／NRR／解約）
3. **企業価値・バリュエーション担当** — KPI→企業価値の接続。シリーズB評価の土台
4. **キャッシュフロー・モニタリング担当** — 希薄化コントロール＝1株あたり価値を守る
5. **資金調達ストーリー担当** — 価値を評価額に変換する語り（シリーズB向け）
6. **事業KPI・ファネル分析担当** — 成長のWhy
7. **商談モニタリング担当** — オペ改革・ninaite.ai ドッグフーディング
8. **内部統制・会計品質チェック担当** — 守り（専門人材採用に合わせて）
9. 労務法務／IPO準備／人事系（将来）

> 全体像と各エージェントの契約は [docs/agent-roster.md](docs/agent-roster.md) を参照。

---

## リポジトリの棚割り

| 場所 | 役割 |
|---|---|
| `CLAUDE.md`（この file） | 全エージェント共通の前提 |
| `.claude/agents/` | 部下（エージェント）の定義書。1体 = 1ファイル |
| `.claude/skills/` | 部下が使う再利用可能な「手順マニュアル」 |
| `docs/` | セマンティック層②の定義（セグメント境界・科目マッピング等） |
| `data/` | データ層。**生データはgitignoreで非公開**。定義・サンプルのみ追跡 |

生の資料（役会資料・議事録・スプレッドシート実物）は GitHub ではなく Google Drive / Notion に置く。
